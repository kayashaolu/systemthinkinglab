# Judgment export — SLUG

<!-- Rendered by the `/scaffold export` ritual (see SKILL.md's "Excerpt and
     export" section). Saved to
     scaffold-wiki/artifacts/judgment-export-YYYY-MM-DD-<slug>.md
     Nothing about this ritual sends anything anywhere — it produces this one
     file, on the learner's own machine, and they decide what happens to it. -->

**Attestation:** this file was rendered by the scaffold agent at the
learner's explicit request via `/scaffold export` on YYYY-MM-DD, from the
entries named below, copied verbatim from `scaffold-wiki/` with nothing
reconstructed, summarized, paraphrased, or edited for the export, and no
other part of the wiki included. It is **not independently signed or
timestamped** — this file alone is not proof of anything. What it's for:
it can be checked against the learner's own `scaffold-wiki/` files, which
remain the source of truth.

**Entries selected by the learner:**

<!-- e.g. "learner/idempotency.md, evidence line dated 2026-08-19"
     "judgment-log.md, entry dated 2026-08-19: <title>" -->

<!-- When a judgment-log entry is selected as a receipt for graded work,
     select its paired learner/ page evidence line too (same concept) — the
     judgment-log entry carries no mastery level by design, and the
     learner-page line is where that level lives. See judgment-log.md's own
     note on this pairing. -->

-

---

<!-- Each named entry, copied verbatim below, in the order selected. A
     heading per entry naming its source file and date keeps the export
     self-describing even out of context. -->
